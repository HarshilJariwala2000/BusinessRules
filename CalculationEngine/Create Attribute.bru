meta {
  name: Create Attribute
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/attribute/create
  body: json
  auth: inherit
}

body:json {
  {
    "name":"mrp",
    "data_type":"numeric"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}
